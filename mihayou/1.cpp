#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  vector<vector<int>> input;
  for (int i = 0; i < n; i++) {
    int l, r;
    cin >> l >> r;
    input.push_back({l, r});
  }
}